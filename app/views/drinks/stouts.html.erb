<div id="stout">
<div class="wrapper">
<% @stouts['result'].each do |product| %>
		  <section class="beers">
		    <%= link_to product['name'], drink_path(product['id']) %>
		  </section>
		<% end %>
		</div>
</div>